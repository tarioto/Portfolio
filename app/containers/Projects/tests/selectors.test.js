// import { fromJS } from 'immutable';
// import { makeSelectProjectsDomain } from '../selectors';

// const selector = makeSelectProjectsDomain();

describe('makeSelectProjectsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
